VITE_API_URL=http://localhost:3000/api/v1

# ─── Supabase (pilote migration 100% Supabase) ───────────────────────────────
# Project Settings > API. La clé anon est publique par conception (protégée
# par RLS côté base) — ne jamais y mettre la clé service_role.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
